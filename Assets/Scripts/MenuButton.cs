using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuButton : MonoBehaviour
{
    //private AnimationClip[] animationClip;
    //public Animation animation;
    // Start is called before the first frame update
    void Start()
    {
        /*animation = GetComponent<Animation>();
        animation.Stop();*/
        //animator = GetComponent<Animator>();
        //animationClip = animator.runtimeAnimatorController.animationClips;
        //Debug.Log("该对象有" + animationClip.Length + "个动画");
        //foreach (AnimationClip a in animationClip)//遍历获取所有该对象的动画名
        //{
        //    Debug.Log(a.name);
        //}

    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void huodong()
    {
        //animator = GetComponent<Animator>();
        //Transform[] obj = transform.GetComponentsInRealChildren<Transform>();
        //Debug.Log(animator);
        //animationClip = animator.runtimeAnimatorController.animationClips;
        //Debug.Log("该对象有" + animationClip.Length + "个动画");
        //Debug.Log("gwyegqwyuieg");
        //animator.Play(animationClip[0].name);
        //animator.Update(0);
    }
}
